# This tree is published as an npm package; anything uninvited here ships to
# every consumer. A macOS .DS_Store slipped in once exactly this way.
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
node_modules/
*.tgz
